{
  "name": "@ousi-ui/cli",
  "version": "0.4.0",
  "description": "CLI tool for Ousi UI",
  "type": "module",
  "bin": {
    "ousi-ui": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "echo \"skip: cli build not configured\"",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ousi-ui/registry": "workspace:*"
  },
  "devDependencies": {
    "@ousi-ui/tsconfig": "workspace:*",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-dts": "^4.3.0"
  }
}
